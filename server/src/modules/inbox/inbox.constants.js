const CONVERSATION_REPLY_STATES = [
  'Awaiting reply',
  'Unread',
  'New lead',
  'Follow up today',
]

const MESSAGE_SENDERS = ['contact', 'user', 'system']

module.exports = {
  CONVERSATION_REPLY_STATES,
  MESSAGE_SENDERS,
}
